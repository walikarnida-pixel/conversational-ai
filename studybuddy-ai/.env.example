# OPTIONAL — the app works fully offline without this.
# To enable AI-enhanced flashcards: copy this file to ".env", add your key, and
# run:  pip install openai python-dotenv
OPENAI_API_KEY=
MODEL_NAME=gpt-4o-mini
